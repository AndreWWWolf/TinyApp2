<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
  <% include partials/_header %>
  <% if(user && user.id === userID) { %>
  <div class='px-5 m-4 bg-success border rounded float-center clearfix'>
    <h3>Code: <a href="../u/<%= shortURL %>"> u/<%= shortURL %> </a>will currently link to: <%= longURL %></h3>
    <!-- for METHOD OVERRIDE USE:
    <form class="form-inline" action="/urls/<%- shortURL%>?_method=PUT" method="POST">
    instead of next form line -->
    <form class="form-inline" action="/urls/<%- shortURL%>" method="POST">
      <div class="form-group mb-2">
        <label for="longURL">Please Enter URL you want this code to link to: </label>
        <input class="form-control" type="text" name="longURL" placeholder="http://" style="width: 300px; margin: 1em">
        <button type="submit" class="btn btn-dark">Submit</button>
        </div>
    </form>
  </div>
  <% } else if(user && user.id !== userID)  {%>
    <div class="shadow p-3 mb-5 bg-warning rounded p-5 m-5">
      <h3> This is not your shortURL. You cannot edit this shortURL.</h3>
    </div>
  <% } else { %>
    <div class="shadow p-3 mb-5 bg-warning rounded p-5 m-5">
      <p>Please register or log in to your account to view or create new URLs</p>
    </div>
  <% } %>
</body>
</html>